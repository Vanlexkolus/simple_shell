#include "shell.h"

/**
 * display_prompt - This is a function that writes the name of
 * the shell to the standard output followed by a \"$\" sign
 * letting the active user know it's ready to receive and process
 * commands
 * Return: void
 */

void display_prompt(void)
{
	_print("Endurance and Alex shell$");
}
